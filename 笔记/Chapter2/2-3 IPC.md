[TOC]


# 2-3 进程间交流 

## 线程间交流方式分类

### 通过通过共享内存进行交流

### 通过发送消息进行交流

## 通过共享内存进行交流

### 带来的问题

#### 互斥问题（早期单处理器多线程时期）：如何避免多个进程同时访问一个资源

#### 同步问题（多处理器多线程）：如何确保进程之间既不同时访问一个资源又按照顺序完成任务

### 引入概念：原子操作（Atomic Operation）

#### 定义

#### 例子

### 解决互斥问题（早期并发控制）

#### 忙等待（Buzy Waiting）
通过不断的检查标志量来查看

##### 锁变量(Lock Variables)


##### 严格交替(alternation)

##### 彼德森算法

##### 硬件层的支持

###### 禁用处理器中断解决单处理器上的同步，用于操作系统内核的极小criticalSection。

###### TSL(Test-and-Set-Lock)基于CPU指令的同步方法。

#### 休眠法(Slesp And Awake)

### 解决同步问题（多处理器多线程并发控制）

#### 使用信号量提供 “操作系统API级别” 的对同步的支持

#### 使用Monitor提供编译器自动实现的 “编程语言级别” 的对同步的支持


-------------------

## 通过发送消息进行交流

### 消息传递解决不同进程中的线程的交流

<!-- TODO: 介绍一下消息传递主要是服务什么样的线程之间的通信。 -->

### 操作系统API提供支持

#### 两个libc函数通过封装syscall实现消息传递。

#### 操作系统底层对消息传递的支持


### 消息的结构

### 发送方与接收方的同步：send{block,nonlock},receiver{block,nonblock} send x receiver - {(nb,b)}

### 消息传递的方式：直接/间接

### 
















