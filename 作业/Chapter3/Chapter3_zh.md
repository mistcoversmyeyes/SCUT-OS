## P254

### 3. 
一个交换系统通过紧缩来消除空洞。假设存在许多空洞和许多数据段的随机分布，并且读取或写入一个 32 位内存字的时间为 4 纳秒，那么紧缩 4GB 大约需要多长时间？为简单起见，假设字 0 是空洞的一部分，并且内存中的最高字包含有效数据。

### 4. 
考虑一个交换系统，其内存按顺序包含以下大小的空洞：10 MB、4 MB、20 MB、18 MB、7 MB、9 MB、12 MB 和 15 MB。对于连续的段请求：
(a) 12 MB  
(b) 10 MB  
(c) 9 MB  
首次适应算法会选择哪个空洞？现在用最佳适应算法、最差适应算法和下次适应算法重复此问题。

### 6. 
对于以下每个十进制虚拟地址，计算 4KB 页和 8KB 页的虚拟页号和偏移量：20000、32768、60000。

### 7. 
使用图 3-9 的页表，给出与以下每个虚拟地址对应的物理地址：  
(a) 20  
(b) 4100  
(c) 8300  

### 17. 
假设一台机器有 38 位虚拟地址和 32 位物理地址。  
(a) 多级页表相对于单级页表的主要优点是什么？  
(b) 对于两级页表、16KB 页面和 4 字节条目，应为顶级页表字段分配多少位，为下一级页表字段分配多少位？请解释。

## P259

### 45. 
解释内部碎片和外部碎片之间的区别。哪一种发生在分页系统中？哪一种发生在纯分段系统中？

### 47. 
我们考虑一个程序，它有两个段，如下所示，段 0 中是指令，段 1 中是读/写数据。段 0 具有读/执行保护，段 1 仅具有读/写保护。内存系统是一个按需分页的虚拟内存系统，虚拟地址具有 4 位页号和 10 位偏移量。页表和保护如下所示（表中的所有数字均为十进制）：

## P333

### 12. 
描述给定文件的损坏数据块对以下情况的影响：  
(a) 连续分配  
(b) 链接分配  
(c) 索引（或基于表的）分配  

### 18. 
考虑一个文件，其大小在其生命周期内从 4 KB 到 4 MB 不等。三种分配方案（连续、链接和表/索引）中哪一种最合适？

### 20. 
两名计算机科学专业的学生 Carolyn 和 Elinor 正在讨论 i 节点。Carolyn 认为内存已经变得如此之大且如此便宜，以至于当打开一个文件时，获取 i 节点的新副本到 i 节点表中比搜索整个表以查看它是否已存在更简单、更快。Elinor 不同意。谁是对的？

### 21. 
说出硬链接相对于符号链接的一个优点，以及符号链接相对于硬链接的一个优点。